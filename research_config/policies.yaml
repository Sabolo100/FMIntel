dedupe:
  strategy: "name_similarity"
  similarity_threshold: 0.85
  unique_keys:
    - "name"

persistence:
  default_policy: "insert_if_new"
  review_threshold: 0.6

approval:
  auto_approve_above: 0.8
  require_manual_below: 0.4
